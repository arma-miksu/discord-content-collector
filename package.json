{
  "name": "discord-content-collector",
  "version": "0.1.0",
  "description": "Collects content from Discord channels.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon -w src --ext ts --exec \"tsc && node dist/index.js\"",
    "build": "tsc",
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "test:dev": "nodemon -w src --ext ts --exec \"mocha -r ts-node/register src/**/*.spec.ts\"",
    "start": "node dist/index.js"
  },
  "keywords": [
    "discord"
  ],
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "nodemon": "2.0.4",
    "prettier": "2.0.5",
    "ts-mockito": "^2.6.1"
  },
  "dependencies": {
    "@types/nconf": "^0.10.0",
    "@types/node": "^14.0.11",
    "discord.js": "^12.2.0",
    "nconf": "^0.11.4",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
